type InitialState = {
  showEmoji: boolean;
  mode: "dark" | "light";
  show_user_profile: boolean;
  show_profile_upload_modal: boolean;
  show_profile_drop_down: boolean;
};

export { InitialState };
